# PRD — montesprout-site

> **Placeholder, deliberately.** This repo has no product intent of its own to freeze, so `/adopt` did
> not invent one. This file exists so the `PROGRESS.md` router resolves and so a fresh session knows
> where intent actually lives.

**The site has no separate PRD, and shouldn't.** It is one deliverable of the MonteSprout product; its
intent of record is the app's:

- **Product intent (immutable):** `~/Developer/MonteSprout/docs/PRD.md`
- **Why the site exists and what it must say:** `docs/DECISIONS.md` here — the site-repo route, the
  no-design-round call, and the privacy-copy contract.
- **The evidence the landing copy is built on:** `~/Developer/MonteSprout/docs/research/montessori-software-gaps.md`
  — the incumbents' 1.2★/1.9★ iOS reviews complain about capture friction, which is why the page sells
  the ten-second moment rather than a feature list.
- **Paste-ready privacy + support copy:** `~/Developer/MonteSprout/docs/plans/2026-08-16-external-testing-drafts.md`
  §§1–2, *with its own warning attached*: it tells its reader not to trust it without re-checking each
  claim against the code. Three of its claims were wrong by the time the pages were written.

If the site ever acquires intent the app's PRD doesn't cover — its own audience, its own success
measure, a school-facing product surface — that is the moment to write a real PRD here, and it is a
decision to record in `docs/DECISIONS.md` first.
